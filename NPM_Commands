#to start your project
npm start
#to remove all your node_modules 
rm -rf node_modules
#to install packages in packages.json file
npm install --save
#for only update packages-lock.json file
npm install --package-lock-only
